/**
 * Default Settings Configuration
 *
 * These values will be used when the app first starts and no settings are saved.
 * To set a permanent default API URL, update the DEFAULT_API_URL below.
 *
 * Note: Once a user saves settings, these defaults will NOT override their saved values.
 */

export const DEFAULT_API_URL = "http://192.168.103.219:3000";
export const DEFAULT_DEMO_MODE = 0; // 0 = off, 1 = on
