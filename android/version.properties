# Each release build (assembleRelease / bundleRelease) auto-increments:
#   VERSION_NAME patch: 1.0.1 -> 1.0.2 (last numeric segment only)
#   VERSION_CODE: +1 (Play Store)
# No manual edit required for normal releases.
VERSION_CODE=2
VERSION_NAME=1.2.1
